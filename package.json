{
  "name": "a-eq-b",
  "module": "src/index.ts",
  "dependencies": {
    "rimraf": "^6.1.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "@types/bun": "^1.3.12",
    "copyfiles": "^2.4.1",
    "pug": "^3.0.4",
    "pug-cli": "^1.0.0-alpha6",
    "serve": "^14.2.6",
    "tailwindcss": "^4.2.4",
    "typescript": "^6.0.3"
  },
  "private": true,
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "tsc && pug ./src --out ./dist --hierarchy && copyfiles res/* dist/ && copyfiles -u 1 src/**/*.css dist && tailwindcss -i ./src/style.css -o ./dist/style.css",
    "serve": "serve dist",
    "dev": "bun run build && bun run serve"
  },
  "type": "module"
}
